## Dependency change summary

Base branch: ``

This PR updates the resolved gem set based on changes in `Gemfile` / `Gemfile.lock` (and related gemspec changes, if any).

### Direct dependencies (from Gemfile)

| Gem | Old | New | Change | Groups | Risk |
|-----|-----|-----|--------|--------|------|
| `activerecord` | – | 4.1.0 | added | default | Medium |
| `activesupport` | – | 4.1.0 | added | default | Medium |
| `factory_girl` | – | 4.4.0 | added | default | Medium |
| `faker` | – | 1.3.0 | added | default | Medium |
| `pg` | – | 0.17.1 | added | default | Medium |
| `rack-test` | – | 0.6.2 | added | default | Medium |
| `rake` | – | 10.3.1 | added | default | Medium |
| `rspec` | – | 2.14.1 | added | default | Medium |
| `shotgun` | – | 0.9 | added | default | Medium |
| `shoulda-matchers` | – | 2.5.0 | added | default | Medium |
| `sinatra` | – | 1.4.5 | added | default | Medium |
| `sinatra-contrib` | – | 1.4.2 | added | default | Medium |
| `thin` | – | 1.6.2 | added | default | Medium |

### Transitive / stdlib gems (from Gemfile.lock only)

| Gem | Old | New | Change | Groups | Risk |
|-----|-----|-----|--------|--------|------|
| `activemodel` | – | 4.1.0 | added | runtime? | Medium |
| `arel` | – | 5.0.1.20140414130214 | added | runtime? | Medium |
| `backports` | – | 3.6.0 | added | runtime? | Medium |
| `builder` | – | 3.2.2 | added | runtime? | Medium |
| `daemons` | – | 1.1.9 | added | runtime? | Medium |
| `diff-lcs` | – | 1.2.5 | added | runtime? | Medium |
| `eventmachine` | – | 1.0.3 | added | runtime? | Medium |
| `i18n` | – | 0.6.9 | added | runtime? | Medium |
| `json` | – | 1.8.1 | added | runtime? | Medium |
| `minitest` | – | 5.3.3 | added | runtime? | Medium |
| `multi_json` | – | 1.9.2 | added | runtime? | Medium |
| `rack` | – | 1.5.2 | added | runtime? | Medium |
| `rack-protection` | – | 1.5.3 | added | runtime? | Medium |
| `rspec-core` | – | 2.14.8 | added | runtime? | Medium |
| `rspec-expectations` | – | 2.14.5 | added | runtime? | Medium |
| `rspec-mocks` | – | 2.14.6 | added | runtime? | Medium |
| `thread_safe` | – | 0.3.3 | added | runtime? | Medium |
| `tilt` | – | 1.4.1 | added | runtime? | Medium |
| `tzinfo` | – | 1.1.0 | added | runtime? | Medium |

_Generated by gem_diff GitHub Action._

----

@copilot

Please help with the following, based on the dependency changes above:

1. **Fix failing CI/CD workflows** caused by these dependency upgrades.
   - Identify where these gems are used in the codebase.
   - Update or add code to keep existing behavior working for current versions.
   - For any version-specific changes (new APIs, callbacks, controller changes, configs):
     - Implement the necessary code updates.
     - Add comments explaining what changed in the upgraded dependency version.
     - Link to the relevant changelog entries.

2. **Changelog and docs updates**:
   - Update or create `docs/dependency-upgrades-changelog.md` (or similar) describing:
     - Which dependencies changed and why.
     - What code paths or features were updated.
     - Any behavioral differences users or developers should know about.

3. **Dependency pattern review**:
   - Check that any version-conditional code follows our `dependency_upgraded_next` pattern.

4. **PR handling**:
   - Open a draft PR with these changes first, then later mark it 'ready for review'.
   - Ensure all CI checks pass before marking the PR ready.
